import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Products | Add",
  description: "Add a new product page.",
};

function AddProductsPage() {
  return <div>Add Product Page</div>;
}

export default AddProductsPage;
